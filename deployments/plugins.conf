# plugins.conf — tells Fluent Bit where to find the jti_nus plugin.
# Copy to /etc/fluent-bit/plugins.conf
# (or add the Path line to your existing plugins.conf)

[PLUGINS]
    Path /usr/local/lib/fluent-bit/jti_nus_fluentbit.so
